// read every line of the <input recordfile> sequentially and compare it to the <search word>
int SequentialSearch();
// uses a binary search function
int InterpolationSearch();
// array cleanup func
void cleanup(char** arr, int arr_len);